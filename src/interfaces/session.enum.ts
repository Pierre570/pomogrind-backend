export enum typeSession {
  TIMER = 'timer',
  UNLIMITED = 'unlimited',
}

export enum statusSession {
  ACTIVE = 'active',
  TERMINATED = 'terminated',
  PAUSED = 'paused',
  ABANDONED = 'abandoned',
}
